apiVersion: v1
kind: Service
metadata:
  name: homepage
  namespace: homepage
  labels:
    app: homepage
spec:
  type: LoadBalancer
  loadBalancerIP: 192.168.0.203
  externalTrafficPolicy: Local
  selector:
    app: homepage
  ports:
    - name: http
      port: 80
      targetPort: 3000
